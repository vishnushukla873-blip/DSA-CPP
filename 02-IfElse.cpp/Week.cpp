#include<iostream>
using namespace std;
int main()
{
        int days;
        cout<<"Enter the days";
        cin>>days;
        if(days = 1)
        {
            cout<<"Monday";
        }
        else if(days=2)
        {
            cout<<"Tuesday";
        }
        else if(days=3)
        {
            cout<<"Wednseday";
        }
        else if(days=4)
        {
            cout<<"Thursday";
        }
       else if(days=5)
       {
        cout<<"Friday";
       }
       else if(days=6)
       {
        cout<<"Saturday";
       }
       else if(days=7)
       {
        cout<<"Sunday";
       }
       else
       {
        cout<<"Invalid days";
       }
       return 0;







}